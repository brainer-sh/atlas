/* Adds two integers. */
int add(int a, int b) {
    return a + b;
}

/* Computes the sum of two values. */
int compute(int a, int b) {
    return add(a, b);
}

typedef struct Point {
    float x;
    float y;
} Point;

#define MAX_POINTS 64
